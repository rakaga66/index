/*
 * Fill these fields with IDs from your approved Google AdSense account.
 * The ad spaces stay hidden and no Google ad request is made while values
 * are empty, so never replace them with sample IDs.
 */
window.QAAF_ADSENSE_CONFIG = Object.freeze({
    publisherId: 'ca-pub-4756596092432831',
    topSlotId: '7557631167',
    bottomSlotId: '2481312733'
});
